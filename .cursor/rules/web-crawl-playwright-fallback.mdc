---
description: 用户要求从 URL 抓取内容并生成/改写 MingoX 文章时，若内置网页抓取失败、返回验证页或明显反爬，则改用本仓库 Playwright 脚本由用户在本地执行并读产物文件。
alwaysApply: false
---

# 网页抓取：Playwright 回退（本仓库）

## 何时使用

当任务需要**根据外链正文**写作或改编 `posts/*.html`，且出现以下任一情况时，**优先建议并采用本方式**（勿反复空试内置抓取）：

- 返回「环境异常」、验证码、登录墙、空正文；
- 域名典型反爬：**微信公众号** `mp.weixin.qq.com`、部分门户/财经站；
- 用户明确说「反对爬取」「要用浏览器抓」等。

## 做法

1. 让用户在本机仓库根目录安装依赖（仅需一次）：
   - `pip install -r util/requirements-crawl.txt`（或 `pip install playwright`）
   - `playwright install chromium`
2. 运行（微信文章建议**不要**加 `--headless`，必要时用户在窗口内完成验证）：
   - `python util/crawl-with-playwright.py --url "<完整URL>"`
3. 成功后脚本会打印 `meta` 与 `html` 路径；用 **Read** 工具读取：
   - `util/.crawl-output/last.meta.json`（标题、作者、url）
   - `util/.crawl-output/last_content.html`（`#js_content` 内 HTML，供改编正文）
4. 据此撰写双语 HTML 时仍遵守 [docs/EDITORIAL.md](../../docs/EDITORIAL.md) 中的词汇与结构规范；**注意版权**：转载与商用须合规，仅作参考改写时需把握尺度。

## 非微信页面

当前选择器针对**公众号文章 DOM**（`.rich_media_title`、`#js_name`、`#js_content`）。其他站点若失败，需在 `util/crawl-with-playwright.py` 中扩展选择器或模式，勿臆造正文。
