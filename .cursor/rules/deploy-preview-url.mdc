---
description: 每次远程部署完成后须在对话中粘贴完整预览 URL（含查询串）
alwaysApply: true
---

# EdgeOne 远程部署 → 完整预览链接

**每次**助手在本仓库执行 **`python3 workflow/mingox.py deploy`**（或其它 EdgeOne 远程部署）且命令**成功结束**后，在**同一条或紧随其后的回复**里，**必须**向用户粘贴**一行完整预览 URL**：

- 与终端里 **「预览地址」** 下一行，或 **`EDGEONE_DEPLOY_URL=`** 后的值**完全一致**。
- **必须包含** `https://`、路径，以及 **`?` 之后的全部查询参数**（如 `eo_token`、`eo_time`）；不得截断、不得改写为「仅域名」或「请到终端复制」代替。
- 可另起一行简短提醒：该链接含访问令牌，**勿在公开渠道传播**。

不得仅用「部署成功」而不给出上述完整链接。
