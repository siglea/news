---
description: mingox deploy 成功后须在对话中写出完整预览 URL
alwaysApply: true
---

# EdgeOne 预览地址

当对话涉及 **`mingox deploy` / EdgeOne 部署成功**时，助手**必须**在回复中写出**完整预览地址**（与终端「预览地址」下一行或 `EDGEONE_DEPLOY_URL=` 后一致，**包含 `?` 及 `eo_token`、`eo_time` 等全部查询参数**）。不得仅用「已部署成功」代替链接。勿在公开渠道传播含令牌的 URL。
