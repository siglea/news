# 《mihoyo-liuwei-entrepreneurship-caiaoyu》错误复盘（逐段）

## 1) 事实与可核验性风险

- 段落 1-7：导语包含较多结论性转述（如“命运的齿轮开始转动”），但仅有单一来源链接，缺乏交叉核验来源。
- 段落 41、75、113、145、177：图片说明式短段落混入正文流程，来源上下文不完整，易被误读为可独立验证事实。

## 2) 时效表达与语义漂移

- 段落 3：出现“昨天”。
- 段落 41：出现“本月初”。
- 这类相对时间词在发布日期之后会失去明确指代，需要在编辑阶段改为绝对时间或增加上下文锚点。

## 3) 结构冗余与信息重复

- 段落 9-15：先给出“三点建议”预告。
- 段落 115-223：再次完整展开“三点建议”。
- 段落 215-223：第三次总结“三点建议”。
- 以上三处重复导致信息增量下降，建议将导语与结尾压缩，保留一次完整展开。

## 4) 非正文污染（已修复）

- 原段落 225-237 为招聘、导流、公众号关注文案，不属于学习正文。
- 已从 `01-source.md` 删除，避免进入标注与成稿流程。

## 5) 标注质量错误（已修复）

- 原 `llm_annotations.json` 存在占位符输出（`termN` / `[term]`）和截断锚点（如半截短语）。
- 已按 `llm-chat-bundle.json` 逐句重建标注：192 句中 70 句给出有效词汇项，其余使用 `skip`，避免伪标注污染展示层。

## 6) 项目级流程缺口与本次整改

- 缺口 A：缺少占位符自动拦截，导致低质量标注可进入构建。
  - 已新增标注质量门禁脚本，并接入 `build`。
- 缺口 B：`meta_description` 允许为空，影响发布摘要质量。
  - 已补齐本稿 `meta_description`，并在门禁中增加非空校验。
- 缺口 C：`02-annotate-tasks.json` 文件名与用途不一致（看似任务，实为构建产物）。
  - 已在构建产物新增 `kind: "annotate_result"`，并同步文档澄清其角色。
